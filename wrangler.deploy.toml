# 生产部署：`npx wrangler deploy --config wrangler.deploy.toml`
# Git/CI 请指向本文件；不要用 wrangler pages deploy 部署本 Worker。
#
# 机密：ARK_API_KEY（火山方舟）；选用 Nano banana 2 时另配 TOAPIS_API_KEY

name = "ai-poster"
main = "worker/entry.ts"
compatibility_date = "2025-04-01"

[assets]
directory = "./dist"
binding = "ASSETS"
