# 默认配置：供 `wrangler pages dev`（含 `npm run dev`）读取。
# Wrangler 4+ 不支持 `--config` 自定义路径；且「代理 Vite」时不要写 pages_build_output_dir，否则会与目录模式冲突。
# 生产 Worker：wrangler.deploy.toml

name = "ai-poster"
compatibility_date = "2025-04-01"
