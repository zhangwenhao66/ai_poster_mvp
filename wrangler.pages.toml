# 仅用于「Cloudflare Pages」本地预览：wrangler pages dev dist --config wrangler.pages.toml
# 生产若用 Workers + Static Assets（wrangler deploy），请用根目录 wrangler.toml。

name = "ai-poster"
compatibility_date = "2025-04-01"
pages_build_output_dir = "dist"
